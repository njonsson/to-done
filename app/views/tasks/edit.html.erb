<% content_for(:page_title) do -%>Editing &ldquo;<%= h truncate(@task.name)%>&rdquo;<% end -%>
<%= content_tag :h1, 'Editing task' %>

<%= render :partial => 'form',
           :locals => {:form_action => task_path(@task),
                       :form_method => :put,
                       :submit_button_label => 'Save changes'} %>
